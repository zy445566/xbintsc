我需要实现一个typescript的二进制编译器
实现步骤(LLVM和C++部分)：
        1. 实现JS的各个基础类别(基础类型，function，等等)的实现(以便后续的IR的绑定)
        2. 需要实现对typescript解析成AST树
        3. 通过LLVM实现IR的绑定
        4. 使用llc编译出typescript的二进制编译器
        5. 再使用typescript的二进制编译器实现测试用例的case
要求：
    1. 需要实现增量编译
    2. 合理的规划每个模块(如不同类别的解析和实现都需要按不同的目录存储)
    3. 需要有大量测试用例验证，测试用例也需要按模块划分
    4. 要支持扩展模块作为可选编译模块(比如后续我要将nodejs的模块（如fs模块）或者bun.js的模块也可以正常编译)
    5. 我需要后续能够实现自举(最后可以用编译器来编译一个用ts重新写的编译器)
    6. 要做多系统的支持至少要支持windows,mac,Ubuntu,以及不同系统下的不同芯片(使用github action来实现产物的编译)