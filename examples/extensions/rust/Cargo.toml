[package]
name = "mathx"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "mathx"
# A static library lets xbintsc link the Rust code straight into the binary.
crate-type = ["staticlib"]

[profile.release]
opt-level = 3
lto = true
